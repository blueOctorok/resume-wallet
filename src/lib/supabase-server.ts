/**
 * Server Supabase client (cookie session). Canonical implementation:
 * @/utils/supabase/server — re-exported here for Phase 1 auth track (T1.2+).
 */
export { createClient } from '@/utils/supabase/server'
